/**
 * API entry point.
 *
 * Serves monthly emissions by scope, incident trends, and the data-quality report.
 *
 * Implemented in step 6 — see PLAN.md.
 */

export async function start(): Promise<void> {
  throw new Error('API not implemented yet (PLAN.md step 6)');
}

await start();
